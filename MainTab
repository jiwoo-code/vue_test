<template>
  <div class="sy-main-tab">
    <div class="main-header">
      <div>
        <h3>ER Log Timeline</h3>
        <p class="lede">Click a lot bar to open the detail tab.</p>
      </div>
    </div>

    <div class="legend-row">
      <div class="legend">
        <div class="legend-item">
          <span class="swatch has-sy"></span>
          <span>Has SY Data (avg >= 63)</span>
        </div>
        <div class="legend-item">
          <span class="swatch incomplete"></span>
          <span>Has Incomplete SY Data (avg < 63)</span>
        </div>
        <div class="legend-item">
          <span class="swatch reject"></span>
          <span>Has Reject Wafer (>= 1)</span>
        </div>
      </div>
    </div>

    <SyErLogChart :items="erLogData" @select-lot="onSelectLot" />
  </div>
</template>

<script>
import SyErLogChart from './SyErLogChart.vue';

export default {
  name: 'MainTab',
  components: {
    SyErLogChart
  },
  props: {
    erLogData: {
      type: Array,
      default: () => []
    }
  },
  methods: {
    onSelectLot(payload) {
      this.$emit('select-lot', payload);
    }
  }
};
</script>

<style scoped>
.sy-main-tab {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.main-header {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: flex-start;
  justify-content: space-between;
}

h3 {
  margin: 0 0 6px;
  font-size: 20px;
}

.lede {
  margin: 0;
  color: #cbd5f5;
  opacity: 0.9;
}

.legend-row {
  display: flex;
  justify-content: flex-end;
}

.legend {
  display: flex;
  flex-wrap: wrap;
  gap: 10px 16px;
  font-size: 12px;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8px;
  color: #e2e8f0;
}

.swatch {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  display: inline-block;
}

.swatch.has-sy {
  background: #3b82f6;
}

.swatch.incomplete {
  background: #f59e0b;
}

.swatch.reject {
  background: #ef4444;
}
</style>
